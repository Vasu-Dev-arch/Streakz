# This file is intentionally left empty.
# All environment variables are defined in .env.local (local dev) 
# and in the Vercel dashboard (production).
#
# DO NOT add NEXT_PUBLIC_API_URL here — the app uses same-origin /api/* routes.
