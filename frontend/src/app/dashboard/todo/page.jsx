export const metadata = {
  title: 'To-Do',
  description: 'Manage your tasks and to-dos with Streakz.',
};

import { TodoPageClient } from '../../../components/views/TodoPageClient';

export default function TodoPage() {
  return <TodoPageClient />;
}
